proc foo (val int m, ref float n) 
int tove;
float x[1..8,2..4];
read tove;
x[8,2] := " this is a string \n";
tove := 2*x[1,2] +- 4.2;
If y > 0 Then
    y := tove + 4.2;
    Else
    y := tove;
Fi
write tove;
#lkads
end